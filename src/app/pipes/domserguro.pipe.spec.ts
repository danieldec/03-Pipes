import { DomserguroPipe } from './domserguro.pipe';

describe('DomserguroPipe', () => {
  it('create an instance', () => {
    const pipe = new DomserguroPipe();
    expect(pipe).toBeTruthy();
  });
});
